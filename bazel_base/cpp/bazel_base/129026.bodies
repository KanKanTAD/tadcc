class MultiValue
!!!152450.cpp!!!	append(in obj : SingleValue) : void

  if (obj == nullptr) {
    return;
  }
  obj->father = (Com *)(this);
  this->children.push_back(obj);
!!!153218.cpp!!!	stringify(in out : ostream) : void

  out << "[";
  bool first = true;
  for (const auto &value : this->children) {
    if (!first)
      cout << ", ";
    first = false;
    value->stringify(out);
  }
  out << "]";
